---
title: Users
sidebar_position: 2
persona: iam
description: Create and manage users in Thunder, including profile attributes and group membership.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Manage Users

A user in Thunder represents an account that can authenticate with your applications. Each user has:

- A unique **ID** generated by Thunder on account creation.
- A [user type](./user-types) (for example, `customer` or `employee`) that determines the profile attributes, validation rules, and registration settings for that user.
- A set of attributes that match the schema defined by their user type.
- An [organization unit](../organization-units) that defines the user's place in the organizational hierarchy.

## Onboard a User

Thunder gives you two ways to onboard a user, depending on whether you want to create the account directly or let the user complete their own registration.

<Tabs>
<TabItem value="create" label="Create a User" default>

Use this when you want to create a fully provisioned account on behalf of a user.

1. Navigate to **Users** in the Thunder Console and click **Add User**.
2. Select a **User Type** to determine the user's attribute schema.
3. Select an **Organization Unit** that determines the user's position in the organization hierarchy.
4. Fill in the user attributes.
5. Click **Create User**.

:::tip
The attributes available depend on the selected user type. See [User Schema Reference](./user-schema-reference) to understand the defaults, or [User Types](./user-types) to create your own.
:::

</TabItem>
<TabItem value="invite" label="Invite a User">

Use this when you want the user to complete their own registration. Thunder generates an invite link that you share with them.

1. Navigate to **Users** and click **Invite User**.
2. Select the **User Type** for the invited user.
3. Enter the user's **Email** address.
4. Click **Next**. Thunder generates an **Invite Link**.
5. Copy the link and share it with the user. The link takes them to a registration flow where they complete their profile.

:::tip
The attributes available on the registration form depend on the selected user type. See [User Schema Reference](./user-schema-reference) to understand the default user types. See [User Types](./user-types) to create your own user type.
:::

</TabItem>
</Tabs>

## Update a User

1. Open the user from the **Users** list.
2. Edit the attributes you want to change.
3. To update group membership, add or remove groups on the **Groups** tab.
4. Click **Save**.

## Delete a User

1. Open the user from the **Users** list.
2. Click **Delete** and confirm.

:::warning
Deleting a user is permanent. The user's account, attributes, and group memberships are removed immediately.
:::

## Related Guides

- [User Types](/guides/guides/users/user-types) - Define schemas and attribute rules for each user type
- [Organization Units](/guides/guides/organization-units) - Manage the organizational hierarchy that contains users
- [Flows](/guides/guides/flows) - Configure registration flows for self-service sign-up
